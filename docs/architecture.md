# MCP Gateway 아키텍처 문서

**버전**: 1.0.0  
**작성일**: 2026-03-31

---

## 1. 아키텍처 개요

### 1.1 설계 원칙

MCP Gateway는 **URL Redirect 방식 (Reverse Proxy)** 을 채택합니다.

- Gateway 자체에 MCP SDK 불필요
- 개발자 PC의 MCP 서버(stdio)는 그대로 유지
- 환경변수(`CONTEXT7_API_URL`, `EXA_BASE_URL`)만 Gateway로 리다이렉트

이 방식의 장점:
- 순수 HTTP 트래픽으로 콘텐츠 필터링 가능 (HTTPS 암호화 우회 불필요)
- 기존 MCP 서버 수정 없이 중계 가능
- 중앙 집중 보안 정책 관리

---

## 2. 전체 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────────┐
│  개발자 PC (Windows 11, 폐쇄망)                                      │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  opencode (AI 코딩 어시스턴트)                                │   │
│  │                                                              │   │
│  │  ├── context7-mcp (stdio process)                           │   │
│  │  │   CONTEXT7_API_URL=http://gateway:8000/proxy/c7         │   │
│  │  │   CONTEXT7_API_KEY={api-key}                            │   │
│  │  │                                                          │   │
│  │  └── exa-mcp-server (stdio process)                        │   │
│  │      EXA_API_KEY={api-key}                                 │   │
│  │      EXA_BASE_URL=http://gateway:8000/proxy/exa            │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ HTTP (아웃바운드 전용)
                               │ port 8000
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  MCP Gateway (RHEL VM, Podman)                                       │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  FastAPI :8000                                               │    │
│  │                                                              │    │
│  │  ┌─────────────┐   ┌──────────────┐   ┌──────────────────┐ │    │
│  │  │  API Key    │──►│  요청 필터  │──►│  Reverse Proxy   │ │    │
│  │  │  인증       │   │  필터        │   │  (httpx)         │ │    │
│  │  └─────────────┘   └──────────────┘   └────────┬─────────┘ │    │
│  │                                                 │           │    │
│  │                                        ┌────────▼─────────┐ │    │
│  │                                        │  응답 필터      │ │    │
│  │                                        │  필터            │ │    │
│  │                                        └────────┬─────────┘ │    │
│  │                                                 │           │    │
│  │  ┌──────────────────────────────────────────────▼─────────┐ │    │
│  │  │  DB 로깅 (search_logs + audit_trail)                    │ │    │
│  │  └─────────────────────────────────────────────────────────┘ │    │
│  │                                                              │    │
│  │  REST API: /api/v1/logs, /filters, /audit, /users, /stats   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │  Next.js :3000  │  │  PostgreSQL :5432 │  │  Keycloak :8080  │   │
│  │  보안 포털      │  │  로그/규칙/감사  │  │  포털 IDP        │   │
│  └─────────────────┘  └──────────────────┘  └──────────────────┘   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ HTTPS (아웃바운드 전용, 방화벽 허용)
                               ▼
                    ┌─────────────────────────┐
                    │  context7.com           │
                    │  api.exa.ai             │
                    └─────────────────────────┘
```

---

## 3. 요청 처리 흐름

### 3.1 정상 검색 (차단 없음)

```
Developer ──► POST /proxy/c7/v2/libs/search?query=fastapi
                    │
               ┌────┴────┐
               │API Key  │──── invalid ──► 401 Unauthorized
               │  인증   │
               └────┬────┘
                    │ valid (client_ip 추출)
               ┌────┴──────────┐
               │요청 필터 필터│──── 민감정보 없음 ──► pass
               └────┬──────────┘
                    │
               ┌────┴──────────┐
               │ Reverse Proxy │──► HTTPS ──► context7.com/api/v2/libs/search
               │   (httpx)     │◄── 200 OK ◄── upstream response
               └────┬──────────┘
                    │
               ┌────┴──────────┐
               │응답 필터 필터│──── 민감정보 없음 ──► pass
               └────┬──────────┘
                    │
               ┌────┴────┐
               │  DB 로그 │──► search_logs (filtered=false)
               └────┬────┘    audit_trail (action=search)
                    │
                    ▼
Developer ◄── 200 OK + 검색 결과
```

### 3.2 민감정보 포함 검색 (차단 적용)

```
Developer ──► GET /proxy/exa/search?query=10.10.20.30+서버+에러
                    │
               ┌────┴────┐
               │API Key  │ ──── valid
               └────┬────┘
                    │
               ┌────┴──────────┐
               │요청 필터 필터│
               │               │── 사설IP 패턴 매치 ──► "차단+서버+에러"
               │ 원본 텍스트   │
               │ 기록 후 치환  │
               └────┬──────────┘
                    │ (차단된 쿼리로 전달)
               ┌────┴──────────┐
               │ Reverse Proxy │──► api.exa.ai/search?query=차단+서버+에러
               └────┬──────────┘
                    │
               ┌────┴──────────┐
               │응답 필터 필터│──── 응답 내 IP 패턴 치환
               └────┬──────────┘
                    │
               ┌────┴────┐
               │  DB 로그 │──► search_logs (filtered=true, filter_details 포함)
               └────┬────┘    audit_trail (action=search, filter_blocked=true)
                    │
                    ▼
Developer ◄── 200 OK + 검색 결과 (IP 차단된 상태)
              (차단 사실 알림 없음)
```

---

## 4. 차단 필터 파이프라인

```
┌─── 요청 필터 ──────────────────────────────────────────────────┐
│                                                                   │
│  요청 쿼리/본문 ──► regex 규칙 적용 ──► 매칭된 텍스트 치환       │
│                ──► keyword 규칙 적용 ──► 매칭된 단어 치환         │
│                                                                   │
│  치환 전 원본 보관 ──► DB filter_details에 기록                   │
│  치환된 쿼리 ──► 업스트림 전달                                    │
└───────────────────────────────────────────────────────────────────┘

┌─── 응답 필터 ──────────────────────────────────────────────────┐
│                                                                   │
│  응답 본문 ──► regex 규칙 적용 (response/both 방향)               │
│           ──► keyword 규칙 적용 (response/both 방향)              │
│                                                                   │
│  차단된 응답 ──► 개발자에게 전달                                │
└───────────────────────────────────────────────────────────────────┘
```

---

## 5. 컨테이너 구성 (Podman Compose)

```
┌─────────────────────────────────────────────────────────────────┐
│  podman network: mcp-gateway-net                                │
│                                                                 │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   │
│  │ postgres │   │ keycloak │   │ backend  │   │ frontend │   │
│  │ :5432    │   │ :8080    │   │ :8000    │   │ :3000    │   │
│  │          │   │          │   │          │   │          │   │
│  │ RHEL UBI9│   │ RHEL UBI9│   │ RHEL UBI9│   │ RHEL UBI9│   │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘   │
│       ▲               ▲              ▲                         │
│       │               │              │                         │
│  backend이 DB 사용   backend가      frontend가                  │
│                   Keycloak JWKS    backend API 사용             │
│                   조회             (포털 데이터)                │
└─────────────────────────────────────────────────────────────────┘
```

### 컨테이너 이미지

| 서비스 | 베이스 이미지 | 포트 |
|--------|-------------|------|
| postgres | `postgres:15-alpine` | 5432 |
| keycloak | `quay.io/keycloak/keycloak:23` | 8080 |
| backend | `registry.access.redhat.com/ubi9/python-311` | 8000 |
| frontend | `registry.access.redhat.com/ubi9/nodejs-20` | 3000 |

---

## 6. 인증 구조

```
┌─────────────────────────────────────────────────────────────────┐
│  인증 주체별 분리                                               │
│                                                                 │
│  개발자 PC (MCP 서버)                                           │
│  └── API Key 인증 (x-api-key 헤더)                             │
│      └── user_id = "ip:{client_ip}"                            │
│          username = "{client_ip}"                              │
│                                                                 │
│  보안 담당자 (웹 포털)                                          │
│  └── Keycloak OIDC/PKCE 인증 (JWT Bearer)                      │
│      └── user_id = Keycloak sub (UUID)                         │
│          username = "admin-user"                               │
│          roles = ["admin"]                                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 데이터 흐름 (DB)

```
프록시 요청 발생
     │
     ├── search_logs 레코드 생성
     │   ├── user_name: 클라이언트 IP
     │   ├── request_body: 원본 검색어 (차단 전)
     │   ├── filtered: true/false
     │   └── filter_details: { matches: [{ rule_name, matched_text }] }
     │
     └── audit_trail 레코드 생성
         ├── action: "search"
         ├── details.filter_blocked: true/false
         └── details.masked_rules: ["사설 IP 대역", ...]

차단 규칙 변경 발생
     │
     └── audit_trail 레코드 생성
         ├── action: "filter_create" / "filter_update" / "filter_delete"
         └── details: { rule_id, rule_name, changes: {...} }
```

---

## 8. 보안 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│  네트워크 경계                                                   │
│                                                                 │
│  인바운드: 차단 (방화벽)                                         │
│  아웃바운드 허용: context7.com:443, api.exa.ai:443              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  MCP Gateway 내부                                        │   │
│  │                                                         │   │
│  │  API Key → 개발자 PC 접근 허용 (프록시 엔드포인트만)      │   │
│  │  JWT → 보안 담당자 접근 허용 (전체 API)                   │   │
│  │                                                         │   │
│  │  차단 필터 → 외부 전송 전 민감정보 제거                  │   │
│  │  DB → 원본 기록 (내부 네트워크만 접근 가능)               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. 확장 계획

| 항목 | 현재 | 향후 |
|------|------|------|
| 지원 서비스 | Context7, Exa | 추가 MCP 서비스 (`/proxy/{service}/*`) |
| 인증 | API Key + Keycloak | LDAP 연동 고려 |
| 차단 | Regex + Keyword | ML 기반 민감정보 탐지 |
| 모니터링 | 자체 로그 | Prometheus/Grafana 연동 |
| HA | 단일 VM | 다중 인스턴스 + 로드밸런서 |

---

## 10. 참조 문서

| 문서 | 위치 |
|------|------|
| 제품 명세서 | `docs/product-specification.md` |
| Gateway 서버 구축 가이드 | `docs/gateway-setup.md` |
| 개발자 PC 설정 가이드 | `docs/developer-setup.md` |
| 보안 담당자 매뉴얼 | `docs/security-officer-manual.md` |
| 개발자 공지 | `docs/developer-notice.md` |
| 차단 규칙 가이드라인 | `docs/filter_rules.md` |
| 보안 아키텍처 | `architecture/04-security-agent.md` |
| AI 개발 워크플로우 | `AGENTS.md` |
